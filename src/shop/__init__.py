"""A deliberately small but multi-layered application.

EcoCI needs a demo repository where a pull request can plausibly be
documentation-only, UI-only, database-only or security-relevant, so that test
selection has something meaningful to select. Each module below maps to one scope
class and has its own tests.
"""

__version__ = "0.1.0"
